import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.parejaconsciente.app',
  appName: 'Pareja Consciente',
  webDir: 'dist'
};

export default config;
